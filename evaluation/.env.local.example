# Local Model Configuration for Evaluation
# Copy this file to .env and update values as needed

# ============================================
# vLLM Server Configuration
# ============================================
# URL for the local vLLM server (OpenAI-compatible API)
VLLM_BASE_URL=http://localhost:8000/v1

# API key for vLLM server (must match the key used when starting vLLM)
VLLM_API_KEY=token-abc123

# ============================================
# Model Configuration
# ============================================
# Model name for LLM inference (should match the model served by vLLM)
MODEL=Qwen/Qwen2.5-7B-Instruct

# ============================================
# Embedding Configuration
# ============================================
# URL for the vLLM embedding server (separate from LLM server)
VLLM_EMBEDDING_BASE_URL=http://localhost:8001/v1

# Embedding model name (should match the model served by vLLM embedding server)
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-0.6B

# Toggle between vLLM embeddings (true) or local transformers-based embeddings (false)
# - true: Uses vLLM server on port 8001 (faster, uses DP across 8 GPUs)
# - false: Loads model directly via transformers (slower, uses single GPU)
USE_VLLM_EMBEDDINGS=true

# ============================================
# Optional: Override OpenAI settings globally
# ============================================
# These settings will make all OpenAI client instances use the local vLLM server
# Useful if you want to run other evaluation scripts without modifications
OPENAI_BASE_URL=http://localhost:8000/v1
OPENAI_API_KEY=token-abc123

# ============================================
# Legacy Settings (for reference only)
# ============================================
# These are no longer needed for local models but kept for compatibility
# EMBEDDING_MODEL=text-embedding-3-small  # Not used with local embeddings
# OPENAI_API_KEY=sk-...  # Not needed when using local vLLM

# ============================================
# Mem0 Configuration (if using Mem0 API)
# ============================================
MEM0_API_KEY=your-mem0-api-key
MEM0_PROJECT_ID=your-mem0-project-id
MEM0_ORGANIZATION_ID=your-mem0-organization-id

# ============================================
# Zep Configuration (if using Zep)
# ============================================
# ZEP_API_KEY=your-zep-api-key
